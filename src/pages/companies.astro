---
import SectionLayout from '../layouts/SectionLayout.astro';
---

<SectionLayout
  slug="companies"
  kicker="Capstone"
  title="Company map"
  dek="Every company mentioned across the site, mapped to the layer(s) it operates in — colocation landlord, neocloud, hyperscaler, chipmaker, inference platform, model lab."
>
  <div class="callout-note">
    Skeleton page — the filterable company directory is built in Stage 4, once every section (and the
    companies it references) exists.
  </div>
</SectionLayout>
